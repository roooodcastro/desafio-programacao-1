<%= title Purchase.model_name.human.pluralize %>

<div class="card card-body mb-3">
  <div class="row">
    <div class="col">
      <%= link_to t('messages.purchase_new_button'), new_purchase_path,
                  class: 'btn btn-primary px-5' %>
    </div>
  </div>
</div>

<%= render partial: 'purchases/purchases', locals: { purchases: @purchases } %>